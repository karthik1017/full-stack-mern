/*
  url (uniform resource locator) -  
  http://dct-api-data.herokuapp.com/products.json?category=Grocerycategory=Grocery&price=desc
  
  http (hypertexxt transfer protocol) - protocol

  domain name - herokuapp

  tld (top level domain) - .com
  
  sub domain - dct-api-data

  uri (uniform resource identifier) - products

  request format - json

  parameters - parameters start from a ?
  each parameter is separated with an & 
  parameter will be like key = value
  category its value  = grocery
  price its value = desc
*/